@model Projeto_Lunary.Models.Restaurante
@{
    ViewBag.Title = "Editar";
}

<h2>Editar</h2>

<div>
    <form action="/Adiministrador/Editar?id=@Model.RESTAUID" enctype="multipart/form-data" method="post">
        <label for="nome">Nome</label>
        <input type="text" class="form-control" maxlength="100" id="nome" name="nome" value="@Model.RESTANOME" required />
        <div>
            <label for="preco">Preço</label>
            <input type="text" class="form-control" id="" name="preco" value=@Model.RESTAPRECO required />
        </div>
        <div>
            <label for="descricao">Descrição</label>
            <input type="text" class="form-control" id="" name="descricao" value="@Model.RESTADESCRICAO">
        </div>
        <div>
            <label for="precopromocional">Preço Promocional</label>
            <input type="text" class="form-control" id="" name="precopromocional" value=@Model.RESTAPREPROMOCAO>
        </div>
        <div>
            <br />
            <label for="categoria">Categoria: </label>
            <select id="categoria" name="categoria">
                @foreach (var item in ViewBag.listacategoria)
                {
                    <option>@item.RESTACATEGORIA</option>
                }
            </select>
        </div>

        <div>
            <label for="imagem"></label>
            <input type="file" class="form-control" id="imagem" name="imagem" value=@Model.imagem>
            <td>
                @{
                    if (Model.imagem != null)
                    {
                        var base64 = Convert.ToBase64String(Model.imagem);
                        var imgSrc = String.Format("data:image/jpg/gif/png;base64,{0}", base64);
                        <img src='@imgSrc' style="max-width:100px; max-height:100px;" />
                    }
                }
            </td>
        </div>
        <br />
        <div>
            <a href="/Adiministrador/Index"> <button type="button" class="btn btn-primary">Voltar</button>  </a>
            <button type="submit" class="btn btn-success">Salvar</button>
        </div>

    </form>

</div>




